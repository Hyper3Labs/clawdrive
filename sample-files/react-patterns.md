# React Design Patterns

## Compound Components
Split complex components into smaller pieces that share implicit state. Example: Tabs with TabList, Tab, TabPanel.

## Render Props
Pass a function as a prop that returns React elements. Useful for sharing cross-cutting concerns like data fetching.

## Custom Hooks
Extract stateful logic into reusable functions. Hooks like useDebounce, useLocalStorage, useMediaQuery.
